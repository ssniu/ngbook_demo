import { ValidsDirective } from './valids.directive';

describe('ValidsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidsDirective();
    expect(directive).toBeTruthy();
  });
});
